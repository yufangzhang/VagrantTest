server {
  listen 80;
  server_name _;
  root /var/www/app;
  index index.html;

  location / {
    try_files $uri /index.html;
  }

  location ~ \.php$ {
    fastcgi_pass 127.0.0.1:9000;
    fastcgi_index index.html;
    include fastcgi_params;
  }
}
